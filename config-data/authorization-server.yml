spring:
  application:
    name: authorization-server

server:
  port: 9000

logging:
  level:
    org:
      springframework:
        security: ERROR, TRACE


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka/
